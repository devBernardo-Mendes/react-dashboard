// -------------------------------------------------
// packages
// -------------------------------------------------
import { ReactNode } from "react";

export interface IContentHeaderProps {
  title: string;
  lineColor: string;
  children: ReactNode;
}
